import { services as auth } from '@/modules/auth'
import { services as watchlist } from '@/pages/watchlist'
import { services as watchedlist } from '@/pages/watchedlist'
import { services as serieslist } from '@/pages/series'

export default {
  auth,
  watchlist,
  watchedlist,
  serieslist
}
