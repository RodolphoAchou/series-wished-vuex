import { store as auth } from '@/modules/auth'
import { store as watchlist } from '@/pages/watchlist'
import { store as watchedlist } from '@/pages/watchedlist'
import { store as serieslist } from '@/pages/series'

export default {
  auth,
  watchlist,
  watchedlist,
  serieslist
}
